<% @plots.each do |plot| %>
  <div class="plot" id="plot-<%= plot.id %>">
    <h3>Plot Number: <%= plot.number %></h3>
    <ul>
      <% plot.plants.each do |plant| %>
        <li class="plant-name"><%= plant.name %></li>
      <% end %>
    </ul>
  </div>
<% end %>
